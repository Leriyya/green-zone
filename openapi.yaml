openapi: 3.1.0
info:
  title: GreenZonePC API
  description: REST API for the GreenZonePC store — consumed by the customer-facing frontend.
  version: 1.0.0

servers:
  - url: /api
    description: Same-origin (via nginx reverse proxy)

paths:
  /products:
    get:
      summary: List active products
      operationId: listProducts
      parameters:
        - name: category
          in: query
          description: Filter by product category
          required: false
          schema:
            $ref: '#/components/schemas/ProductCategory'
        - name: search
          in: query
          description: Full-text search on product name / description
          required: false
          schema:
            type: string
        - name: page
          in: query
          description: Page number (1-based, default 1). Page size is 20.
          required: false
          schema:
            type: integer
            minimum: 1
            default: 1
      responses:
        '200':
          description: Paginated list of active products
          content:
            application/json:
              schema:
                type: object
                required: [products, page]
                properties:
                  products:
                    type: array
                    items:
                      $ref: '#/components/schemas/Product'
                  page:
                    type: integer
                    example: 1
        '500':
          $ref: '#/components/responses/InternalError'

  /products/{id}:
    get:
      summary: Get a single product
      operationId: getProduct
      parameters:
        - $ref: '#/components/parameters/ID'
      responses:
        '200':
          description: Product object
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /builds:
    get:
      summary: List active PC builds
      operationId: listBuilds
      parameters:
        - $ref: '#/components/parameters/Lang'
      responses:
        '200':
          description: List of active PC builds
          content:
            application/json:
              schema:
                type: object
                required: [builds]
                properties:
                  builds:
                    type: array
                    items:
                      $ref: '#/components/schemas/PCBuild'
        '500':
          $ref: '#/components/responses/InternalError'

  /builds/{id}:
    get:
      summary: Get a single PC build
      operationId: getBuild
      parameters:
        - $ref: '#/components/parameters/ID'
        - $ref: '#/components/parameters/Lang'
      responses:
        '200':
          description: PC build object
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PCBuild'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

components:
  parameters:
    ID:
      name: id
      in: path
      required: true
      schema:
        type: string
      description: UUID of the resource
    Lang:
      name: lang
      in: query
      required: false
      description: Language code for translated fields (name, description). Falls back to English.
      schema:
        type: string
        enum: [en, de, ru]
        default: en

  responses:
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            error: product not found
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            error: failed to list products

  schemas:
    ProductCategory:
      type: string
      enum:
        - component
        - peripheral
        - accessory

    Product:
      type: object
      required:
        - id
        - name
        - category
        - price_cents
        - stock
        - low_stock_threshold
        - images
        - active
      properties:
        id:
          type: string
          format: uuid
          example: 018f2c3d-1a2b-7000-8c9d-0e1f2a3b4c5d
        name:
          type: string
          example: NVIDIA RTX 4070
        description:
          type: string
          example: High-performance GPU for gaming and content creation.
        category:
          $ref: '#/components/schemas/ProductCategory'
        price_cents:
          type: integer
          description: Price in cents (e.g. 59999 = $599.99)
          example: 59999
        stock:
          type: integer
          example: 12
        low_stock_threshold:
          type: integer
          description: Stock level at or below which the product is considered low-stock
          example: 3
        specs:
          description: Arbitrary key/value spec data (structure varies by product)
          example:
            vram: "12GB"
            tdp: "200W"
        images:
          type: array
          items:
            type: string
            format: uri-reference
          example:
            - /images/abc123.jpg
        active:
          type: boolean
          example: true

    Component:
      type: object
      properties:
        field_name:
          type: string
          description: Display label for this component slot
          example: CPU
        component_name:
          type: string
          description: The component model or name
          example: AMD Ryzen 7 7700X

    BuildComponents:
      type: object
      description: >
        Map of component slot keys to component objects. Well-known keys include:
        cpu, gpu, motherboard, ram, ssd, nvme, hdd, power_supply, case,
        cpu_cooler, liquid_cooler, case_fans, sound_card, network_card.
        Custom keys are also allowed.
      additionalProperties:
        $ref: '#/components/schemas/Component'
      example:
        cpu:
          field_name: CPU
          component_name: AMD Ryzen 7 7700X
        gpu:
          field_name: GPU
          component_name: NVIDIA RTX 4070
        ram:
          field_name: RAM
          component_name: 32GB DDR5-5600

    PCBuild:
      type: object
      required:
        - id
        - name
        - price_cents
        - stock
        - images
        - components
        - active
        - refurbished
        - created_at
      properties:
        id:
          type: string
          format: uuid
          example: 018f2c3d-1a2b-7000-8c9d-0e1f2a3b4c5d
        name:
          type: string
          example: The Gamer Pro
        description:
          type: string
          example: A high-end gaming build ready to ship.
        price_cents:
          type: integer
          description: Original price in cents (e.g. 149999 = €1499.99)
          example: 149999
        new_price_cents:
          type: integer
          nullable: true
          description: Sale/discounted price in cents. Present only when the build is on sale.
          example: 129999
        stock:
          type: integer
          example: 1
        images:
          type: array
          items:
            type: string
            format: uri-reference
          example:
            - /images/build_front.jpg
        components:
          $ref: '#/components/schemas/BuildComponents'
        active:
          type: boolean
          example: true
        refurbished:
          type: boolean
          description: Whether this build uses refurbished parts
          example: false
        featured:
          type: integer
          nullable: true
          description: Display order for featured builds (null if not featured)
          example: 1
        created_at:
          type: string
          format: date-time
          example: "2025-01-15T10:30:00Z"

    Error:
      type: object
      required: [error]
      properties:
        error:
          type: string
          example: product not found
